{
  "name": "mux-clip-api",
  "version": "1.0.0",
  "description": "Production API for Mux video clip extraction — custom, first X, last X minutes",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@mux/mux-node": "^8.3.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  }
}
